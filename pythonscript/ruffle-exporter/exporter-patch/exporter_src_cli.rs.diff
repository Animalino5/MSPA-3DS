--- a/exporter/src/cli.rs
+++ b/exporter/src/cli.rs
@@ -86,6 +86,18 @@
     #[clap(long = "skipframes", default_value = "0")]
     pub skipframes: u32,
 
+    /// Comma-separated list of frame indices to capture (e.g. "0,4,8,12").
+    /// Frames are still simulated in order from 0, but only the listed
+    /// indices are rendered and saved. Takes precedence over --frames.
+    #[clap(long = "frame-indices", value_name = "INDICES", conflicts_with = "frames")]
+    pub frame_indices: Option<String>,
+
+    /// Path to a file containing whitespace/comma-separated frame indices
+    /// to capture (same semantics as --frame-indices, but without any
+    /// command-line length limits — useful for decimating long movies).
+    #[clap(long = "frame-indices-file", value_name = "FILE", conflicts_with_all = ["frames", "frame_indices"])]
+    pub frame_indices_file: Option<PathBuf>,
+
     /// Don't show a progress bar
     #[clap(short, long, action)]
     pub silent: bool,
